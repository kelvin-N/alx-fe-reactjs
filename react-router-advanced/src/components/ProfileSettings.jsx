function ProfileSettings() {
  return (
    <div>
      <h3>Profile Settings</h3>
      <p>This is the profile settings section where users can update their info.</p>
    </div>
  );
}

export default ProfileSettings;
